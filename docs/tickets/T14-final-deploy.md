# T14 — Final deploy + e2e (owner: main + Enzo)

Goal: the deployed product runs the real catalog end-to-end. SPEC §11.14.

Checklist: full env on Railway + Vercel; operator seeded; upload real catalog.csv → stage → reconcile → confirm → generations complete → assets stored → send review → decide on phone → export CSV with working asset links; verify recovery (restart api mid-generation); capture unit-economics numbers for APPROACH.md.
